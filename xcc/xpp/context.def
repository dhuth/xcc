context(start_line,
        (ws,
         newline,
         continue_line,
         directive_include,
         directive_define,
         directive_undef,
         directive_pushdef,
         directive_popdef,
         directive_if,
         directive_ifdef,
         directive_endif,
         directive_for,
         directive_endfor,
         comma,
         identifier,
         lparen,
         rparen,
         sq,
         dq))

context(sq_continue, (sq_char, sq))
context(dq_continue, (dq_char, dq))
context(lt_continue, (lt_char, gt))
